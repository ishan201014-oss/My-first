import type { Metadata } from 'next';
import { BarChart3, ShieldAlert, Star, Trash2, UsersRound } from 'lucide-react';
import { samplePrompts, users } from '@/lib/sample-prompts';
export const metadata: Metadata = { title: 'Admin Dashboard' };
export default function AdminPage() { const top = samplePrompts.slice(0, 10); return <main className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><p className="text-sm font-semibold uppercase tracking-[0.3em] text-neon-blue">Admin</p><h1 className="mt-3 text-4xl font-black">Moderation and analytics</h1><div className="mt-8 grid gap-4 md:grid-cols-4">{[[UsersRound,'Users',users.length],[BarChart3,'Prompts',samplePrompts.length],[Star,'Featured',samplePrompts.filter(p=>p.featured).length],[ShieldAlert,'Reports',0]].map(([Icon,label,value]) => { const I = Icon as typeof UsersRound; return <div key={label as string} className="glass rounded-3xl p-6"><I className="h-6 w-6 text-neon-blue" /><div className="mt-4 text-3xl font-black">{value as number}</div><div className="text-sm text-slate-400">{label as string}</div></div>; })}</div><section className="mt-10 grid gap-6 lg:grid-cols-2"><div className="glass rounded-[2rem] p-6"><h2 className="text-2xl font-bold">Manage Users</h2><div className="mt-4 space-y-3">{users.map((u)=><div key={u.uid} className="flex items-center justify-between rounded-2xl bg-white/5 p-4"><div><div className="font-semibold">{u.username}</div><div className="text-sm text-slate-400">{u.email} · {u.role}</div></div><button className="rounded-xl border border-white/10 px-3 py-2 text-sm">Edit</button></div>)}</div></div><div className="glass rounded-[2rem] p-6"><h2 className="text-2xl font-bold">Manage Prompts</h2><div className="mt-4 space-y-3">{top.map((p)=><div key={p.id} className="flex items-center justify-between gap-3 rounded-2xl bg-white/5 p-4"><div><div className="line-clamp-1 font-semibold">{p.title}</div><div className="text-sm text-slate-400">{p.category} · {p.likes} likes</div></div><div className="flex gap-2"><button className="rounded-xl border border-white/10 px-3 py-2 text-sm"><Star className="h-4 w-4" /></button><button className="rounded-xl border border-red-500/30 px-3 py-2 text-sm text-red-300"><Trash2 className="h-4 w-4" /></button></div></div>)}</div></div></section></main>; }
