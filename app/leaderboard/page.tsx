import type { Metadata } from 'next';
import { Trophy } from 'lucide-react';
import { users, samplePrompts } from '@/lib/sample-prompts';
export const metadata: Metadata = { title: 'Creator Leaderboard' };
export default function LeaderboardPage() { const ranked = users.map((u) => ({ ...u, score: samplePrompts.filter((p) => p.authorId === u.uid).reduce((s,p)=>s+p.likes+p.favorites*2,0) })).sort((a,b)=>b.score-a.score); return <main className="mx-auto max-w-4xl px-4 py-14"><p className="text-sm font-semibold uppercase tracking-[0.3em] text-neon-blue">Leaderboard</p><h1 className="mt-3 text-4xl font-black">Top creators</h1><div className="mt-8 space-y-4">{ranked.map((u,i)=><div key={u.uid} className="glass flex items-center justify-between rounded-3xl p-5"><div className="flex items-center gap-4"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-gradient-to-br from-neon-blue to-neon-purple font-black">#{i+1}</div><div><div className="text-lg font-bold">{u.username}</div><div className="text-sm text-slate-400">{u.badges?.join(' · ')} · {u.followers?.toLocaleString()} followers</div></div></div><div className="font-bold"><Trophy className="mr-2 inline h-5 w-5 text-amber-300" />{u.score.toLocaleString()}</div></div>)}</div></main>; }
