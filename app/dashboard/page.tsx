import type { Metadata } from 'next';
import Link from 'next/link';
import { Heart, LineChart, Plus, Settings, Star, ThumbsUp } from 'lucide-react';
import { samplePrompts, users } from '@/lib/sample-prompts';
import { PromptCard } from '@/components/prompt-card';
export const metadata: Metadata = { title: 'User Dashboard' };
export default function DashboardPage() { const mine = samplePrompts.filter((p) => p.authorId === users[1].uid).slice(0, 6); const favorites = samplePrompts.slice(12, 18); const likes = mine.reduce((s,p)=>s+p.likes,0); return <main className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-[0.3em] text-neon-blue">Dashboard</p><h1 className="mt-3 text-4xl font-black">Creator command center</h1></div><Link href="/submit" className="rounded-full bg-white px-5 py-3 font-semibold text-ink"><Plus className="mr-2 inline h-4 w-4" />Submit Prompt</Link></div><div className="mt-8 grid gap-4 md:grid-cols-4">{[[LineChart,'Total prompts',mine.length],[ThumbsUp,'Likes received',likes],[Star,'Favorites',favorites.length],[Heart,'Followers',6820]].map(([Icon,label,value]) => { const I = Icon as typeof LineChart; return <div key={label as string} className="glass rounded-3xl p-6"><I className="h-6 w-6 text-neon-blue" /><div className="mt-4 text-3xl font-black">{value as number}</div><div className="text-sm text-slate-400">{label as string}</div></div>; })}</div><section className="mt-12"><div className="flex items-center justify-between"><h2 className="text-2xl font-bold">My Prompts</h2><Settings className="text-slate-500" /></div><div className="mt-6 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{mine.map((p,i)=><PromptCard key={p.id} prompt={p} index={i} />)}</div></section><section className="mt-12"><h2 className="text-2xl font-bold">My Favorites</h2><div className="mt-6 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{favorites.map((p,i)=><PromptCard key={p.id} prompt={p} index={i} />)}</div></section><section className="mt-12 glass rounded-[2rem] p-8"><h2 className="text-2xl font-bold">Profile Settings</h2><div className="mt-5 grid gap-4 md:grid-cols-2"><input placeholder="Display name" defaultValue="PixelMage" className="rounded-2xl border-white/10 bg-white/5 text-white" /><input placeholder="Avatar URL" defaultValue={users[1].avatar} className="rounded-2xl border-white/10 bg-white/5 text-white" /><textarea placeholder="Bio" defaultValue={users[1].bio} className="rounded-2xl border-white/10 bg-white/5 text-white md:col-span-2" /></div><button className="mt-5 rounded-2xl bg-gradient-to-r from-neon-blue to-neon-purple px-6 py-3 font-semibold">Save profile</button></section></main>; }
