import Link from 'next/link';
import { Sparkles } from 'lucide-react';
export function Footer() { return <footer className="border-t border-white/10 bg-black/20"><div className="mx-auto grid max-w-7xl gap-8 px-4 py-12 sm:px-6 md:grid-cols-4 lg:px-8"><div className="md:col-span-2"><div className="flex items-center gap-3 text-xl font-bold"><Sparkles className="text-neon-purple" />PromptHub AI</div><p className="mt-4 max-w-md text-sm text-slate-400">Discover production-grade AI image prompts, follow elite creators, and build a reusable prompt library with likes, favorites, analytics, and fast search.</p></div>{[['Product',['Explore','Leaderboard','Submit']],['Company',['About','Privacy','Terms']]].map(([title, links]) => <div key={title as string}><h3 className="font-semibold">{title as string}</h3><div className="mt-3 space-y-2">{(links as string[]).map((link) => <Link className="block text-sm text-slate-400 hover:text-white" href={link === 'Explore' ? '/explore' : '#'} key={link}>{link}</Link>)}</div></div>)}</div><div className="border-t border-white/10 px-4 py-6 text-center text-sm text-slate-500">© 2026 PromptHub AI. Built for Vercel and Firebase.</div></footer>; }
